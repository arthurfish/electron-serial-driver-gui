{
  "main": "build/App.js",
  "scripts": {
    "prestart": "npm run compile",
    "start": "npm run compile && electron ./",
    "compile": "tsc && copyfiles -f src/public/* build/public ",
    "electron": "electron ."
  },
  "dependencies": {
    "copyfiles": "^2.4.1",
    "effect": "^3.8.4",
    "electron": "^32.1.2",
    "serialport": "^12.0.0",
    "typescript": "^5.6.2"
  }
}
